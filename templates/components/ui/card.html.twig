{#
  Card component style shadcn
  Usage: {% include 'components/ui/card.html.twig' with { class: '' } %}
#}
<div class="rounded-lg border bg-card text-card-foreground shadow-sm {{ class|default('') }}">
  {% block content %}{{ content|default('')|raw }}{% endblock %}
</div>
